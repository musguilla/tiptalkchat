import type { SeoPage } from '../seo-pages';

// Per-slug translated SEO content. Empty until filled; missing slugs fall
// back to the Spanish source in seo-pages.ts.
export const ptBr: Record<string, Partial<SeoPage>> = {};
